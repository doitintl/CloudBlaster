package com.doitintl.blaster.test;
/**
 * This package contains an integration test. It is not in a seperate test directory because
 * it is not a unit test and we do not want to run it in CI. It creates and deletes
 * assets, and so is slow and fragile.
 */